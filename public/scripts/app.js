var app = angular.module('volunteerTrack', ['volunteerTrack.controllers', 'volunteerTrack.services'])

  //
  // app.config(['$routeProvider', function ($routeProvider) {
  // //       $routeProvider.when('/users/dashboard', {templateUrl: 'views/partials/opps_list.erb', controller: 'OppsCtrl'});
  //       $routeProvider.when('/users/dashboard/opp_new', {templateUrl: 'views/partials/opp_new.erb', controller: 'UserDetailCtrl'});
  // // //       // // $routeProvider.when('/user-creation', {templateUrl: 'partials/user-creation.html', controller: 'UserCreationCtrl'});
  // // //       // $routeProvider.otherwise({redirectTo: '/user-list'});
  //   }]);
